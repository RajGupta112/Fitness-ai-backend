spring:
  application:
    name: UserService

  datasource:
    url: jdbc:mysql://localhost:3306/fitness_microservice
    username: root
    password: Raj#2024
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    serviceUrl:
     defaultZone: http://localhost:8761/eureka/


server:
  port: 8081